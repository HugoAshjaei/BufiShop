const currencyCodeEnum = ['ریال', 'تومان'];
const adminRoleEnum = ['super-admin', 'admin'];
const storeTypes = ['product', 'service', 'file'];
export {
    currencyCodeEnum,
    adminRoleEnum,
    storeTypes
}
